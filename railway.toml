[build]
builder = "nixpacks"

[deploy]
startCommand = "sh -c 'gunicorn wsgi:app --bind 0.0.0.0:${PORT:-8080} --workers 1 --timeout 120 --keep-alive 5'"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
